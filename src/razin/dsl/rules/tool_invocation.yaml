rule_id: TOOL_INVOCATION
version: 1
metadata:
  title: "Tool invocation tokens in docs"
  description: "Skill references tool invocation tokens."
  recommendation: "Verify tool token permissions and enforce explicit invocation policies. Review destructive and write-tier tokens closely."
  confidence: medium
  classification: informational
scoring:
  base_score: 20
match:
  source: fields
  strategy: token_scan
  prefix_source: config.tool_prefixes
  scan_service_tokens: true
dedupe: true
