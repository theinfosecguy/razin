rule_id: MCP_DENYLIST
version: 1
metadata:
  title: "Denylisted MCP endpoint"
  description_template: "MCP endpoint '{url}' matches denylist policy."
  recommendation: "Remove denylisted MCP endpoint references from docs."
  confidence: high
scoring:
  base_score: 90
match:
  source: fields
  strategy: url_domain_filter
  url_filter: is_mcp_endpoint
  domain_check: is_denylisted
dedupe: true
